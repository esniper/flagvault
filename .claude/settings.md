# Claude Code Settings

## Development Workflow

- Always have a dev server running for testing
- When making changes that require a build:
  1. Kill the dev server first
  2. Run the build
  3. When satisfied, start the dev server again
